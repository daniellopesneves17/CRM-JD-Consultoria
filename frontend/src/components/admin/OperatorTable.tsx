"use client";
// Tabela de operadoras com cadastro rápido, edição de contato e ativação.
import { FormEvent, useState } from "react";
import { Plus, Power } from "lucide-react";
import { Button } from "@/components/ui/Button";

export type Operator={id:string;name:string;logoUrl:string|null;commercialContact:string|null;notes:string|null;active:boolean};
export function OperatorTable({items,onCreate,onPatch}:{items:Operator[];onCreate:(data:{name:string;commercialContact?:string;notes?:string})=>Promise<void>;onPatch:(id:string,data:Partial<Operator>)=>Promise<void>}){const[open,setOpen]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);await onCreate({name:String(form.get("name")),commercialContact:String(form.get("commercialContact")||""),notes:String(form.get("notes")||"")});setOpen(false)}return <div><div className="flex items-center justify-between"><div><h3 className="font-semibold">Operadoras</h3><p className="text-sm text-slate-500">Catálogo usado na importação e nas propostas.</p></div><Button size="sm" onClick={()=>setOpen(value=>!value)}><Plus size={15}/>Nova operadora</Button></div>{open&&<form onSubmit={submit} className="mt-4 grid gap-3 rounded-xl border bg-slate-50 p-4 md:grid-cols-3"><input name="name" required placeholder="Nome" className="h-10 rounded-lg border px-3 text-sm"/><input name="commercialContact" placeholder="Contato comercial" className="h-10 rounded-lg border px-3 text-sm"/><input name="notes" placeholder="Observações" className="h-10 rounded-lg border px-3 text-sm"/><Button size="sm" className="md:col-span-3">Cadastrar</Button></form>}<div className="mt-4 overflow-x-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead className="border-b text-xs uppercase text-slate-500"><tr><th className="py-3">Logo</th><th>Nome</th><th>Contato</th><th>Status</th><th>Ações</th></tr></thead><tbody className="divide-y">{items.map(item=><tr key={item.id}><td className="py-3">{item.logoUrl?<img src={item.logoUrl} alt="" className="h-8 w-8 rounded object-contain"/>:<span className="grid h-8 w-8 place-items-center rounded bg-slate-100 text-xs font-bold">{item.name[0]}</span>}</td><td className="font-medium">{item.name}</td><td>{item.commercialContact||"—"}</td><td><span className={item.active?"text-emerald-600":"text-red-600"}>{item.active?"Ativa":"Inativa"}</span></td><td><Button size="sm" variant="ghost" onClick={()=>onPatch(item.id,{active:!item.active})}><Power size={14}/>{item.active?"Desativar":"Ativar"}</Button></td></tr>)}</tbody></table></div></div>}
