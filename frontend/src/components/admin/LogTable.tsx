// Tabela genérica de logs com paginação server-side.
import { Button } from "@/components/ui/Button";

export type LogColumn<T>={label:string;render:(item:T)=>React.ReactNode};
export function LogTable<T extends {id:string}>({items,columns,page,pages,onPage}:{items:T[];columns:LogColumn<T>[];page:number;pages:number;onPage:(page:number)=>void}){return <div><div className="overflow-x-auto"><table className="w-full min-w-[850px] text-left text-sm"><thead className="border-b bg-slate-50 text-xs uppercase text-slate-500 dark:bg-slate-900"><tr>{columns.map(column=><th key={column.label} className="px-4 py-3">{column.label}</th>)}</tr></thead><tbody className="divide-y dark:divide-slate-800">{items.map(item=><tr key={item.id}>{columns.map(column=><td key={column.label} className="px-4 py-3 align-top">{column.render(item)}</td>)}</tr>)}</tbody></table></div><div className="mt-4 flex items-center justify-end gap-3"><Button size="sm" variant="secondary" disabled={page<=1} onClick={()=>onPage(page-1)}>Anterior</Button><span className="text-xs text-slate-500">Página {page} de {pages}</span><Button size="sm" variant="secondary" disabled={page>=pages} onClick={()=>onPage(page+1)}>Próxima</Button></div></div>}
