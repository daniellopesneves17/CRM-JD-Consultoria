// Barra de ações aplicada aos leads selecionados.
import { Trash2, X } from "lucide-react";import { Button } from "@/components/ui/Button";import type { Stage } from "@/types";
export function BulkActionBar({count,onMove,onDelete,onClear}:{count:number;onMove:(stage:Stage)=>void;onDelete:()=>void;onClear:()=>void}){if(!count)return null;return <div className="sticky bottom-4 z-20 mt-3 flex flex-wrap items-center gap-3 rounded-xl bg-slate-950 p-3 text-white shadow-xl"><strong className="text-sm">{count} selecionado(s)</strong><select onChange={event=>event.target.value&&onMove(event.target.value as Stage)} defaultValue="" className="h-9 rounded-md border-white/20 bg-slate-800 px-3 text-sm"><option value="" disabled>Mover para...</option><option value="QUALIFICADO">Qualificado</option><option value="PROPOSTA_ENVIADA">Proposta enviada</option><option value="EM_ANALISE">Em análise</option><option value="NEGOCIACAO">Negociação</option><option value="FECHADO">Fechado</option><option value="PERDIDO">Perdido</option></select><Button size="sm" variant="danger" onClick={onDelete}><Trash2 size={15}/>Excluir</Button><button onClick={onClear} className="ml-auto rounded p-2 hover:bg-white/10" aria-label="Limpar seleção"><X size={17}/></button></div>}
