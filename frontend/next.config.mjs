// Configuração do Next.js 14. A extensão MJS é exigida por esta versão do framework.
/** @type {import('next').NextConfig} */
const nextConfig = { reactStrictMode: true, turbopack: { root: process.cwd() } };
export default nextConfig;
